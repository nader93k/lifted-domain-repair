After getting the results for BFS, we test running A* with unary relaxation here.
We have h and depth tie breakers implemented.
We set the timeout to 15mins.
Instance IDs tested are limited to Pascal's list. We can run the other instances later if required.


###  Problem count
(planning) nader@planning 9 AStar-full-log length1-15 (nader-h)*
$ find . -maxdepth 1 -name "*.yaml" -o -name "*.yml" | wc -l
511
(planning) nader@planning 9 AStar-full-log length1-15 (nader-h)*
$ cd ../8\ BFS-full-log\ length1-15/
(planning) nader@planning 8 BFS-full-log length1-15 (nader-h)*
$ find . -maxdepth 1 -name "*.yaml" -o -name "*.yml" | wc -l
578